{
  "name": "morningstar-website-unified",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd mstech-app && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "pg": "^8.20.0"
  }
}
